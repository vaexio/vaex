Since git doesn't track .git, these files should go in the directory and hooks for instance can be symlinked:
ln -s git/hook/post-commit .git/hooks/post-commit
ln -s git/hook/post-checkout .git/hooks/post-checkout
